# Go port worklog

skill_use: session-worklog source=agents -> Broad repo conversion with architecture decisions, generated output parity, and proof commands.

User goal:
- Convert the sqlc TypeScript generator implementation to Go in the repository root.
- Use the existing TypeScript generator as the oracle during initial development.
- Delete TypeScript generator/tooling once the Go port is proven.
- Leave room for future output targets such as Bun and rendering/type override options like sqlc-gen-go.
- Before port work, add a Lefthook config matching PDX generic Go formatting/lint/test hooks and run `lefthook install`.

Worktree:
- Working in `/home/tyler/Projects/sqlc-gen-typescript` on `main`.
- The session-worklog skill recommends a task worktree for non-trivial repo work, but the user explicitly asked to build the Go module here in the root directory.

Initial local checks:
- `bun test` passed 7 tests before edits.
- Local tools found: `sqlc v1.31.1`, `just`, `bun`, `javy`, `buf`, `lefthook`, `golangci-lint`, `govulncheck`.
- `sqlc-dev` was not on PATH, so stock `sqlc` should be the proof path where possible.

external_resource: /home/tyler/source/codex-projects/pdx/lefthook.yml -> Generic Go hook shape copied: goimports, modernize, golangci-lint, go test, default configs only.
external_resource: github.com/sqlc-dev/plugin-sdk-go -> Go plugin SDK provides `codegen.Run` plus protobuf request/response types.
external_resource: github.com/sqlc-dev/sqlc-gen-go -> Official generator uses the same Go entrypoint for process and `GOOS=wasip1 GOARCH=wasm` builds.

Architecture:
- Added `docs/architecture/go-port.md`.
- decision: Use `plugin-sdk-go` directly rather than a generic template plugin.
- decision: Keep sqlc request analysis, PostgreSQL type policy, options, validation, and TypeScript rendering separated so future Bun/runtime targets and type overrides can plug in at the model/rendering boundary.

Implementation:
- Added Go module at the repository root.
- Added `cmd/sqlc-gen-typescript/main.go` entrypoint using `codegen.Run(generator.Generate)`.
- Added `internal/generator`, `internal/options`, `internal/model`, `internal/postgres`, `internal/ts`, and `internal/validate`.
- Added Go tests for duplicate-name validation, JSON type mapping, missing metadata, and the authors generated-output oracle.
- Replaced Just targets with Go build/test/lint/format/generate targets.
- Updated CI and Release workflows to use Go instead of Bun, Buf, and Javy.
- Updated `AGENTS.md`, `README.md`, and `llms.txt` for the Go implementation.
- Removed root TypeScript/Javy/buf generator implementation and tooling files.

Proof:
- `lefthook install` -> synced pre-commit hook.
- `go test ./...` -> passed.
- `GOOS=wasip1 GOARCH=wasm go build -o examples/plugin.wasm ./cmd/sqlc-gen-typescript && cd examples && sqlc -f sqlc.dev.yaml generate && git diff -- examples/node-postgres/src/db/query_sql.ts examples/bun-postgres/src/db/query_sql.ts` -> passed with no generated fixture diff.
- `just fmt && just lint && just test && just plugin-wasm && cd examples && sqlc -f sqlc.dev.yaml diff` -> passed; `golangci-lint` reported 0 issues.
- `just build` -> passed; regenerated examples with no tracked fixture diff.
- `git diff --check` -> passed.

Remaining notes:
- `examples/plugin.wasm` is built locally and ignored.
- The first Go port validates reserved option names for `driver`, `runtime`, `bigint`, and `overrides`, but type overrides are intentionally reserved for the next implementation phase.
